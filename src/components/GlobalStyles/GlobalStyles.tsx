import React from 'react';
import './GlobalStyles.css';

type Props = {
  children?: React.ReactNode;
};
function GlobalStyles({ children }: Props): any {
  return children;
}

export default GlobalStyles;
